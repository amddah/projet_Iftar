<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>log in</title>
    <style>
       .container1 {
            width: 400px;
            margin: 0 auto;
            text-align: center;
        }

        form {
            padding: 20px;
            background-color: #f2f2f2;
            border-radius: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 10px;
        }

        #password {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }

        button[type="submit"] {
            width: 100%;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #3e8e41;
        }
    </style>
</head>
<body>
        <div class="container1">
          <form action="/login" id="login-form" method="post" >
            <h2>Connexion</h2>
            <div class="form-group">
              <label for="password">Mot de passe :</label>
              <input type="password" id="password" name="password">
            </div>
            <input type="hidden" id="tester"name="tester"value="0">
            <button type="submit"id="btn" >Se connecter</button>
          </form>
        </div>
             
        <!--script**********************************************************-->
        <script>
            var stockpass="",stockidadmin="";cmpt="0";
            document.getElementById("btn").addEventListener("click", ()=>{
            const password = document.getElementById("password").value;
            if (!password) {
                alert("Veuillez entrer le password.");
                return false;
            }
            else{
                // Envoyer les données au serveur pour vérification...
                '<% for(i=0;i<login.length;i++) {%>'
                    stockpass='<%= login[i].pass%>'
                    if(stockpass==password){
                        cmpt=1;
                        document.getElementById("tester").value="1";
                        return true;
                    }
                '<%}%>'
                    if(cmpt=="0"){
                        alert("password incorrect");
                        return false;
                    }
            }
            });

        </script>
</body>
</html>
    